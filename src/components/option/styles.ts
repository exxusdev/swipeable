import { StyleSheet } from "react-native";

export const stylesOption = StyleSheet.create({
    container: {
        height: 60,
        width: 60,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 6,
    },
})